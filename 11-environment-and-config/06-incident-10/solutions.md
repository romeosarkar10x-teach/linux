# Solutions — 11/06 Incident 10

**Flag:** `KESTREL{the_shell_only_lied_to_the_one_who_owned_it}` (registered as `11/06`)

**Stage tokens** (checkpoints, not flags, never registered):

    1  STAGE{one-directory-two-answers}
    2  STAGE{two-mechanisms-one-file}
    3  STAGE{last-line-wins}

## Establish what is true (1–8)

1. Five: `01-boarding 02-navigation 03-files 04-creating 05-globbing`.
2. Nothing unusual — all owned by `cadet:crew`, ordinary modes. No file is
   `chmod 000`, nothing is a broken symlink, nothing is a dotfile.
3. Yes. `find` lists all five. `find` walks the filesystem itself and never
   asks your shell to expand anything, which is why it is the right first
   instrument in an incident like this.
4. `2186-03-02 08:00:00` — the reorganisation. Nothing in the archive has been
   touched since, so "somebody deleted and restored it" is not available.
5. Swapping chairs. It rules out the terminal, the keyboard and the typo in one
   move, and it is a better experiment than most people would design.
6. —
7. The claim under test is: *two shells, same directory, different results.*
   Not "a directory is missing". Exercise 3 already killed that.
8. Four. `02-navigation` is not listed.

## Checkpoint 1 (9–12)

9. For example:

        ls archive > /tmp/mine
        HOME=$PWD/homes/dorn bash -l -c 'ls "$ARCHIVE"' > /tmp/theirs
        diff /tmp/mine /tmp/theirs

   (`ls` is not aliased in a non-interactive shell, so this shows the
   `GLOBIGNORE` half. See 21.)
10. `02-navigation`.
11. `bin/compare-view 02-navigation` → `STAGE{one-directory-two-answers}`.
12. That two *lists* have to be compared, not two counts. Counting by eye is
    how cass got three identical wrong answers.

## The mechanism (13–24)

13. Yes. `ls -d <literal path>` has no glob in it, so nothing filters it.
14. Yes — `02-navigation` is written literally; only the `*` after it expands,
    and `README`/`deck-map.txt` are not ignored names.
15. No: `echo *` prints four names. The **shell** expands `*`, and it removes
    ignored names before any program sees them. `ls`, `echo` and `rm` all get
    the shortened list and none of them can tell. Exercise 13 works because
    there was nothing for the shell to expand.
16. Yes, fully readable.
17. Nothing is wrong with the directory. One account's shell is configured not
    to produce its name.
18. `homes/dorn/.config/kestrel/env.sh:8`.
19. Because `.bashrc` does not contain it — it *sources* the file that does.
    `grep -rn GLOBIGNORE homes/dorn/` (recursive, and note the dot-directory)
    is what finds it. A single-file grep is a test of one hypothesis; an
    incident needs the recursive one.
20. `.profile` → `.bashrc` → `.config/kestrel/env.sh`.
21. Two lines:

        GLOBIGNORE='02-navigation:02-navigation/'
        alias ls='ls --hide=02-navigation'

    `GLOBIGNORE` removes the name from every glob expansion. The alias covers
    the case where no glob is involved at all — a bare `ls` with no arguments,
    which is what cass ran. Fixing either one alone leaves the other. That is
    why the checker tests both, and why finding one is not finding it.
22. `type ls` → ``ls is aliased to `ls --hide=02-navigation'`` — that is the one
    that tells you the truth. `\ls` and `command ls` both bypass the alias and
    list all five, but they only show you that something is wrong, not what.
    None of them says anything about `GLOBIGNORE`; that half is invisible to
    `type` because it is not a command at all.
23. `bin/name-mechanism GLOBIGNORE .config/kestrel/env.sh` →
    `STAGE{two-mechanisms-one-file}`.
24. The account owns it; the toolchain writes it and never reads it back; it has
    never compared one account's file against another's. So anyone who can write
    to a home directory can put anything in a file that is *presumed generated*,
    and no process on the station will ever notice. "Generated" is a social
    fact, not a technical one.

## The repair (25–33)

26. Because the file's first line says it is regenerated on upgrade. Delete the
    lines and the repair survives exactly until the next upgrade, at which
    point the account quietly goes back to not seeing the archive and nobody
    is watching for it. A repair that an upgrade undoes is a delay.
27. The `.` of `env.sh` happens partway through `.bashrc`; `PS1` is assigned
    after it. So `.bashrc` gets the last word, and lesson 05's rule applies:
    the last assignment in a file wins.
28. Append to `.bashrc`, after the sourcing block:

        # env.sh is regenerated by the toolchain and hides archive section 02.
        # We cannot edit it and keep the fix, so we override it here instead.
        unset GLOBIGNORE

29. `bin/verify-repair` rejects: globbing is fixed, `ls` is not.
30. Because the alias is a second, independent mechanism. `unset GLOBIGNORE`
    says nothing about aliases. Add:

        unalias ls 2>/dev/null

    (`2>/dev/null` because on a shell where the alias was never defined,
    `unalias` complains — and a startup file that prints errors is its own
    small incident.)
31. `bin/verify-repair` → `STAGE{last-line-wins}`.
32. `423edca9f8d10d158b52dbb51727c871` — unchanged.
33. `HOME=$PWD/homes/dorn bash -l`, then `ls "$ARCHIVE"` and `cd "$ARCHIVE";
    echo *`. Five each.

## Close it (34–40)

34–36. `stat -c '%y' homes/dorn/.config/kestrel/env.sh` → `2187-05-19 02:41:00`.
    The comment inside the file says `Generated 2186-03-02`. The comment is a
    claim written by whoever last wrote the file; the mtime is a record kept by
    the filesystem. They disagree by fourteen months. The date to give the
    checker is **2187-05-19**.
37. `bin/incident-close 'STAGE{last-line-wins}' 2187-05-19` prints the closing
    report and the flag.
38. `kestrel flags submit 'KESTREL{the_shell_only_lied_to_the_one_who_owned_it}'`
39. Rejected — stage tokens are not registered. Correct, because a checkpoint is
    a message from the lab to you about your own progress, and a flag is a claim
    you made to the course. Mixing them would let a student "capture" a flag by
    finding a string.
40. Six through chapter 11.

## Write-up (41–50)

41. For example: "The directory was never missing. Your colleague's account has
    a startup file that tells their shell to leave `02-navigation` out of `ls`
    and out of every `*`. The shell did what it was configured to do and neither
    of you was shown any indication that a filter existed."
42. Yes, and she should not have to like it. Directory contents are read by a
    program; what she sees is what her shell chose to pass on. Two shells,
    two configurations, two truthful answers.
43. `find archive -maxdepth 1`, or `ls -d archive/02-navigation`, or `type ls`.
    Not unreasonable to miss: every one of those is a command you only reach for
    once you already suspect your own tools, and suspecting your own tools is
    the last thing anyone does.
44. Yes. The upgrade rewrites `env.sh` with the same two lines; `.bashrc` still
    sources it and still overrides it afterwards. That is the point of fixing it
    downstream rather than upstream.
45. A bare `unset GLOBIGNORE` at the end of a `.bashrc` is a mystery to the next
    reader and will be deleted by someone tidying up. Two comment lines naming
    the file it counteracts and why are part of the repair.
46. (a) `.bashrc` is read by people; `.config/kestrel/env.sh` is read by nobody,
    because everything about it says "generated, don't look". (b) A search for
    the string in the obvious file finds nothing, and dotted config directories
    are skipped by careless greps and by `ls` without `-a`. It is hidden by
    convention, not by permissions — which is why permissions did not help.
47. The account owns it. Who *could* have written it: the account, or root. Who
    *did*: not answerable from what is in this lab, and you should not answer
    it. The timestamp, the fact that it does not match the file's own claim, and
    the fact that it hides exactly one thing are what you report.
48. The toolchain never compares one account's generated file to another's. A
    checksum of every generated file at generation time, compared on upgrade,
    would have caught it — a few lines and a table, and the reason it does not
    exist is that nobody expected the generated file to be interesting.
49. `find` and `grep -r` read directories through the kernel and act on what is
    there. `ls` printed exactly what it was asked to print by a shell that had
    been told to remove one name, with a flag it was handed. Nothing lied.
    `ls` was *given a shorter question*, and answered it accurately.
50. Hidden is a property of the file — a leading dot, a mode, a location.
    Invisible-to-one-person is a property of a *view*, and the file has no idea
    it is happening. Only the second one can be true for one person and false
    for another at the same instant.

## Added exercises 51–52

**51.** With only the first pattern:

```
$ mkdir -p /tmp/g/{02-navigation,a,b}; touch /tmp/g/f1 /tmp/g/.hidden; cd /tmp/g
$ GLOBIGNORE='02-navigation'
$ echo *
.hidden a b f1
$ echo */
02-navigation/ a/ b/
```

`echo *` hides it. `echo */` does **not**. The words `*/` expands to carry a trailing slash, and
`GLOBIGNORE` patterns are matched against the generated word, so `02-navigation/` does not match the
pattern `02-navigation`. The second pattern in the lab's value exists exactly to close that: with
`GLOBIGNORE='02-navigation:02-navigation/'`, both forms are covered.

What that tells you about the author: they did not reason it out from the manual, they **tested it**
— ran the trailing-slash form, saw the directory reappear, and added a second pattern to cover the
case. Two patterns where one would do, differing only in a trailing slash, is the fingerprint of
someone who checked their work against the way people actually list directories. That is a small
piece of evidence about care and intent, and it is worth recording as such and no further: it says
the line was tested, not who tested it.

**52.**

**First — `ls -a` defeats `--hide`.**

```
$ ls -a
.  ..  .hidden  02-navigation  a  b  f1
```

The directory is there. `ls --hide=PATTERN` is documented as being overridden by `-a` and `-A`: the
manual says the option does not apply when `-a` or `-A` is also given. (`--ignore`/`-I` is the
version that is *not* cancelled — the difference between the two options is precisely this.) So the
alias hides the directory from `ls` and from `ls -l`, and gives it straight back to anyone who types
the single most common `ls` option there is. As a hiding place that is weak: it survives only until
someone looks at a directory they think might have dotfiles in it, which is a thing people do without
suspecting anything. It also means the two mechanisms are not equally good — `GLOBIGNORE` is the
robust half of this trace and the alias is the fragile half.

**Second — a glob that matches nothing is loud.**

```
$ ls -d 02*
ls: cannot access '02*': No such file or directory
$ echo $?
2
```

Look carefully at that message: it names the **pattern**, not a filename. Bash found no match, and
with `nullglob` off it passed the pattern through unexpanded; `ls` then tried to open a file
literally called `02*` and failed, at status 2.

Compare exercise 49. There, `ls` reported a directory listing that was complete, true, and missing
one entry — no error, status 0, nothing to notice. Here the same environment produces an error
message and a non-zero status. **This one is loud**, and it is loud for the same reason the other was
quiet: a glob that matches nothing leaves a residue you can see, while a glob that matches four
things out of five leaves no trace of the fifth. The trace hides best against a background of
successes.

**Third — the side effect.**

```
$ GLOBIGNORE='02-navigation'; shopt dotglob
dotglob         on
$ unset GLOBIGNORE; shopt dotglob
dotglob         off
```

Setting `GLOBIGNORE` to a non-empty value switches `dotglob` on, and unsetting it switches it back
off. This is documented bash behaviour and it follows from the design: once `GLOBIGNORE` exists,
`.` and `..` are filtered by an implicit pattern rather than by the leading-dot rule, so every other
dotfile becomes matchable. The visible consequence is in the exercise-51 output above — `echo *`
under `GLOBIGNORE` listed `.hidden`, which it never does otherwise.

So the person hiding one directory also, without asking for it, made **every dotfile in every
directory** show up in that account's globs. That is the opposite of subtle. To anyone paying
attention it would look like `echo *` and `for f in *` suddenly picking up `.bashrc`, `.git`,
`.config` — the sort of thing that breaks a loop noisily and gets investigated. Nobody did notice, in
this lab, for the same reason nobody noticed the missing directory: the account was not used for the
kind of work that would have tripped over it. But it is the loudest thing this trace does, and it is
worth writing down that the mechanism chosen to hide one name had a blast radius the author probably
never measured.

## Note for the instructor

The Chapter 2 archive is the section that vanished. That is not a coincidence
and a student who says so is doing capstone work nine chapters early. Do not
confirm it and do not deny it. Note it and move on.
