#!/usr/bin/env bash
# setup.sh -- 00/01 has no lab directory.
#
# The container does not exist yet at this point in the course; the student is
# working on their host or in the VM. This script exists only so the six-file
# lesson set is complete and `kestrel seed 00/01` does not error.
set -euo pipefail
echo "00/01 has no lab -- answers go in a file called 00-01-answers.md on your host."
