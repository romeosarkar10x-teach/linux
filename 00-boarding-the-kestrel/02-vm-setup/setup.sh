#!/usr/bin/env bash
# setup.sh -- 00/02 has no lab directory.
# The container does not exist yet; all work here happens in the VM and the
# VirtualBox GUI on the host. Present so the six-file lesson set is complete.
set -euo pipefail
echo "00/02 has no lab -- work in the VM. Answers go in ~/00-02-answers.md."
