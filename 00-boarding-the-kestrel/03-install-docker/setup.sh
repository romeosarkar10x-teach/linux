#!/usr/bin/env bash
# setup.sh -- 00/03 has no lab directory.
# The course container is built in 00/04; this lesson installs the engine that
# builds it. All work happens in the VM.
set -euo pipefail
echo "00/03 has no lab -- work in the VM. Answers go in ~/00-03-answers.md."
